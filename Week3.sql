create database Durga_db;
use Durga_db;
create table t1(
name text,
password varchar(10),
salary double
);
select * from t1;
use Durga_db;
create table t2(
emp_id varchar(5),
name text,
password varchar(10),
salary double
);
select * from t2;
use Durga_db;
create table Reg(
emp_id varchar(5),
name text,
password varchar(10),
email text,
mobile char(10)
);
select * from Reg;
select * from student;

